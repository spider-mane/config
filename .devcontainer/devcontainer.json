// For format details, see https://aka.ms/devcontainer.json.
{
  "name": "Config",
  "dockerComposeFile": [
    "../docker-compose.yml",
    "../docker-compose.override.yml"
  ],
  "service": "app",
  "remoteUser": "config",
  "workspaceFolder": "/app",
  "shutdownAction": "none"
}
